import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.yosai.dashboard',
  appName: 'YosaiDashboard',
  webDir: 'build',
  bundledWebRuntime: false,
};

export default config;

